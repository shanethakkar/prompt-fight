# Copy to backend/.env and fill in. NEVER commit the real .env (it is gitignored).
# The Anthropic API key lives ONLY on the backend; the client never sees it.
ANTHROPIC_API_KEY=

# Optional: override the balance.json location (defaults to <repo>/config/balance.json).
# STICKMANCER_BALANCE_PATH=
